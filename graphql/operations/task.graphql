# Tasks - background task management

query ListTasks {
  tasks {
    id
  }
}

mutation CancelTask($id: ID!) {
  cancelTask(id: $id) {
    cancelledId
    error {
      __typename
      ... on OtherUserError {
        code
      }
    }
  }
}
